// Case to improve

class UserService {
  constructor() {
    // ...
  }

  authenticateUser(username, password) {
    // logic authenticated
  }

  sendEmail(user, subject, message) {
    // logic send email
  }
}